#include <studio.h>

        int main()
         {
                int number,x,y);
                scanf("%d". &number);
                printf("the number you typed was %d\n, number):
                x = number * number.
                printf("niber * number os &d\n",
                return o:
}
